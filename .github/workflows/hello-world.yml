name: Hello World Workflow

on:
   workflow_dispatch
  #push:
  #  branches: [ "main" ]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo " Hello, GitHub Actions!"